{
  "name": "kindle-dashboard-server",
  "version": "1.0.0",
  "description": "E-ink dashboard server and image generator for Kindle Touch",
  "main": "local-dashboard-server.js",
  "scripts": {
    "start": "node local-dashboard-server.js",
    "generate": "node generate-flexible-dashboard.js",
    "trmnl:preview": "$npm_package_config_python trmnl-preview.py",
    "trmnl:next": "$npm_package_config_python trmnl-preview.py --next",
    "trmnl:serve": "$npm_package_config_python trmnl-preview.py --serve",
    "test": "node pokemon-service.test.js && node battery-alert.test.js && node staleness-alert.test.js && node bedtime-alert.test.js && node device-alerts.test.js && node trmnl-service.test.js && node transit-service.test.js",
    "test:nodeps": "node battery-alert.test.js && node staleness-alert.test.js && node bedtime-alert.test.js && node device-alerts.test.js"
  },
  "keywords": [
    "kindle",
    "e-ink",
    "dashboard",
    "image-generation"
  ],
  "author": "ClaudeUser",
  "license": "MIT",
  "config": {
    "//": "Preview scripts need Pillow. Matches resolvePythonBin() in config.js: the venv created by `python3 -m venv venv && venv/bin/pip install -r server/requirements.txt`.",
    "python": "../venv/bin/python3"
  },
  "dependencies": {
    "canvas": "^2.11.2",
    "date-fns": "^2.30.0",
    "node-ical": "^0.25.5"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
